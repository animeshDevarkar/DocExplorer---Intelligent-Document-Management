import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DocExplorer AI - Chat with your PDFs",
  description: "Transform PDFs into interactive knowledge bases. Upload your documents, instantly generate embeddings, and ask questions using AI RAG technology.",
  keywords: ["AI", "PDF", "RAG", "DocExplorer", "Knowledge Base", "Chatbot"],
  authors: [{ name: "Animesh" }],
  openGraph: {
    title: "DocExplorer AI",
    description: "Transform PDFs into interactive knowledge bases.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col bg-background text-foreground transition-colors duration-300">
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}

